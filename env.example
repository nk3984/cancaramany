# Local / Cloud Agent environment
# Copy to `.env.local` and fill in values (never commit secrets).

# Mapbox public token (pk.…). Required for the interactive location map.
NEXT_PUBLIC_MAPBOX_TOKEN=
